import { readFile } from 'node:fs/promises';
import { BrowserAgent } from './browser-agent.mjs';
const base=process.env.SUPABASE_URL,key=process.env.SUPABASE_SECRET_KEY,sessionId=process.env.BROWSER_SESSION_ID||process.env.GITHUB_RUN_ID;
const idleMs=Number(process.env.BROWSER_SESSION_IDLE_MS||1200000),pollMs=Number(process.env.BROWSER_SESSION_POLL_MS||1000),commandTimeoutMs=Number(process.env.BROWSER_COMMAND_TIMEOUT_MS||45000);
if(!base||!key||!sessionId) throw new Error('Missing Supabase credentials or session id');
const headers={apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json'};
const agent=new BrowserAgent({headless:process.env.BROWSER_HEADLESS!=='false'}); let lastActivity=Date.now(),stopped=false,humanControl=false,fatalError=null;
async function rest(path,options={}){const r=await fetch(`${base}/rest/v1/${path}`,{...options,headers:{...headers,...options.headers}});const text=await r.text();if(!r.ok)throw new Error(`Supabase ${r.status}: ${text}`);return text?JSON.parse(text):null;}
async function patchSession(body){return rest(`browser_relay_sessions?session_id=eq.${encodeURIComponent(sessionId)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify(body)});}
async function liveViewUrl(){try{return(await readFile('/tmp/browser-live-url.txt','utf8')).trim()||null}catch{return null}}
async function execute(c){const a=c.args||{};switch(c.action){case'start':return agent.start(a.url);case'getPage':return agent.getPage();case'goto':return agent.goto(a.url);case'newPage':return agent.newPage(a.url);case'listPages':return agent.listPages();case'switchPage':return agent.switchPage(a.index);case'fill':{const text=a.text??a.value;if(text===undefined)throw new Error('fill requires args.text or args.value');return agent.fill(a.elementId,text);}case'click':return agent.click(a.elementId);case'press':return agent.press(a.key);case'typeText':{if(a.text===undefined)throw new Error('typeText requires args.text');return agent.typeText(a.text);}case'clickAt':return agent.clickAt(a.x,a.y);case'clickText':{if(a.text===undefined)throw new Error('clickText requires args.text');return agent.clickText(a.text,a.exact??true);}case'scroll':return agent.scroll(a.direction,a.amount);case'setViewport':return agent.setViewport(a.width,a.height);case'humanTakeover':{const url=await liveViewUrl();if(!url)throw new Error('Live View URL is not available');humanControl=true;await patchSession({state:'human',live_url:url,last_error:null});return{state:'human',liveViewUrl:url};}case'resume':{if(!humanControl)throw new Error('Human takeover is not active');humanControl=false;const page=await agent.getPage();return{state:'ready',page};}case'end':stopped=true;humanControl=false;return agent.end();default:throw new Error(`Unknown action: ${c.action}`)}}
function isTimeoutError(error){return error?.code==='BROWSER_COMMAND_TIMEOUT'||error?.name==='TimeoutError'||/Timeout \d+ms exceeded|timed out after \d+ms/i.test(String(error?.message||error));}
async function executeWithTimeout(c){
  if(c.action==='humanTakeover'||c.action==='end') return execute(c);
  let timer;
  const timeout=new Promise((_,reject)=>{timer=setTimeout(()=>reject(Object.assign(new Error(`Command ${c.action} timed out after ${commandTimeoutMs}ms`),{code:'BROWSER_COMMAND_TIMEOUT'})),commandTimeoutMs);});
  try{return await Promise.race([execute(c),timeout]);}
  catch(error){
    if(isTimeoutError(error)){
      humanControl=false;
      try{const page=await agent.recover();error.browserRecovered=true;error.recoveryGeneration=page.generation;}
      catch(recoveryError){error.browserRecovered=false;error.recoveryError=String(recoveryError?.stack||recoveryError);throw Object.assign(error,{fatalBrowserRecovery:true});}
    }
    throw error;
  }finally{clearTimeout(timer);}
}
const initialLiveUrl=await liveViewUrl();
await rest('browser_relay_sessions?on_conflict=session_id',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({session_id:sessionId,state:'ready',heartbeat_at:new Date().toISOString(),live_url:initialLiveUrl,last_error:null,ended_at:null})});
console.log(`SUPABASE_BROWSER_SESSION_READY session_id=${sessionId}`);
try{while(!stopped&&Date.now()-lastActivity<idleMs){await patchSession({heartbeat_at:new Date().toISOString()});const rows=await rest(`browser_relay_commands?session_id=eq.${encodeURIComponent(sessionId)}&status=eq.pending&select=id,command_id,action,args&order=id.asc&limit=50`);const c=humanControl?rows?.find(row=>row.action==='resume'||row.action==='end'):rows?.[0];if(!c){await new Promise(r=>setTimeout(r,pollMs));continue}lastActivity=Date.now();const claimed=await rest(`browser_relay_commands?id=eq.${c.id}&status=eq.pending`,{method:'PATCH',headers:{Prefer:'return=representation'},body:JSON.stringify({status:'running',started_at:new Date().toISOString()})});if(!claimed?.length)continue;await patchSession({state:'busy'});try{const value=await executeWithTimeout(c);const generation=value&&Number.isInteger(value.generation)?value.generation:(value?.page&&Number.isInteger(value.page.generation)?value.page.generation:undefined);await rest(`browser_relay_commands?id=eq.${c.id}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({status:'done',result:value??null,error:null,completed_at:new Date().toISOString()})});const state=stopped?'ended':humanControl?'human':'ready';await patchSession({state,last_error:null,...(generation!==undefined?{generation}:{}),...(stopped?{ended_at:new Date().toISOString()}:{})});}catch(e){const error=String(e?.stack||e);await rest(`browser_relay_commands?id=eq.${c.id}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({status:'error',error,completed_at:new Date().toISOString()})});if(e?.fatalBrowserRecovery){fatalError=`Browser recovery failed after command error: ${error}`;await patchSession({state:'error',last_error:fatalError});throw e;}await patchSession({state:humanControl?'human':'ready',last_error:error});}}}catch(e){fatalError=fatalError||String(e?.stack||e);throw e}finally{if(!stopped)await agent.end().catch(()=>{});const endedAt=new Date().toISOString();await patchSession({state:fatalError?'error':'ended',ended_at:endedAt,...(fatalError?{last_error:fatalError}:{})}).catch(()=>{});await rest(`browser_relay_commands?session_id=eq.${encodeURIComponent(sessionId)}&status=eq.pending`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({status:'error',error:fatalError||'Browser session ended before command execution',completed_at:endedAt})}).catch(()=>{});}
