import assert from 'node:assert/strict';
import fs from 'node:fs/promises';
import path from 'node:path';
import { BrowserAgent } from './browser-agent.mjs';
const outputDir='artifacts-browser-agent-elementid'; await fs.mkdir(outputDir,{recursive:true});
const html=`<!doctype html><html><head><meta charset="utf-8"><title>Browser Agent elementId E2E</title></head><body><label for="message">Message</label><textarea id="message" aria-label="Message"></textarea><button id="submit" type="button">Submit</button><div id="result" aria-live="polite"></div><script>document.querySelector('#submit').addEventListener('click',()=>{document.querySelector('#result').textContent='RESULT:'+document.querySelector('#message').value;});</script></body></html>`;
const dataUrl=`data:text/html;charset=utf-8,${encodeURIComponent(html)}`; const agent=new BrowserAgent({headless:true}); let finalState;
try{const state1=await agent.start(dataUrl);const textbox=state1.elements.find(e=>e.role==='textbox'&&e.editable&&e.label==='Message');assert.ok(textbox,'textbox was not discovered by observePage');await agent.fill(textbox.id,'elementId test success');const state2=await agent.getPage();const button=state2.elements.find(e=>e.role==='button'&&e.text==='Submit');assert.ok(button,'submit button was not discovered after re-observation');await agent.click(button.id);finalState=await agent.getPage();assert.match(finalState.pageText,/RESULT:elementId test success/);await agent.screenshot(path.join(outputDir,'final.png'));await fs.writeFile(path.join(outputDir,'result.json'),JSON.stringify({ok:true,finalState},null,2));console.log('ELEMENTID_E2E_OK');}catch(error){await fs.writeFile(path.join(outputDir,'result.json'),JSON.stringify({ok:false,error:String(error?.stack||error)},null,2));throw error;}finally{await agent.end();}
